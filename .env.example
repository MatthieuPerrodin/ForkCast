# Copier ce fichier en `.env` (jamais commité) et adapter les valeurs.

DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=

# Laisser vide en local (bascule automatique sur SQLite).
# En production, URL de connexion Supabase (Postgres) -- voir docs/03-stack-technique.md.
# Format : postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=
